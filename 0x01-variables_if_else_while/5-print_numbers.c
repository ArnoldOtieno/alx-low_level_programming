#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Prints numbers from zero to 9
 * Return: returns zero for success
 */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
	printf("%d", i);
	i++;
	}
	return (0);
}
