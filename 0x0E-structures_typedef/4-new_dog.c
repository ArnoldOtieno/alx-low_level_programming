#include <stdlib.h>
#include "dog.h"
/**
 * str_len - gives out the length of a string
 * @s: string to be counted
 * Return: returns length
 */
int str_len(char *s)
{
	int i;
	for (i = 0; s[i] != '\0'; i++)
		return (i);

}


